using UnityEngine;

public class CameraSettings : MonoBehaviour
{
    [Header("Global Camera Settings")]
    [Tooltip("Global glow multiplier for UI and Neon effects.")]
    public float globalGlowIntensity = 2.5f;
}